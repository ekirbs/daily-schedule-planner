// page loads
// time and all stored items are displayed (always an empty slot is rendered, if necessary)
// time slots color changes vs current time

// user adds new input.
// when user pressed save button, the new input is saved to the DOM in local storage for pull on next page load
// even after button press, all items are displayed.

// 